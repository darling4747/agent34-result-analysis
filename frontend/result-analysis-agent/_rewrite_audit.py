import pathlib

FE = pathlib.Path(r"c:\Users\HP Victus\OneDrive - Vignan University\Pictures\project\frontend\result-analysis-agent\src")
LF = chr(10)
BT = chr(96)
DOLLAR = chr(36)

lines = [
    "import { useEffect, useState } from 'react';",
    "import { Shield, RefreshCw, CheckCircle2, XCircle, Clock } from 'lucide-react';",
    "import { PageHeader } from '@/components/common/PageHeader';",
    "import { DataTable, type Column } from '@/components/common/DataTable';",
    "import { EmptyState } from '@/components/common/EmptyState';",
    "import { ErrorState } from '@/components/common/ErrorState';",
    "import { TableSkeleton } from '@/components/common/LoadingSkeleton';",
    "import { getAuditLogs } from '@/api/authApi';",
    "",
    "interface AuditEntry {",
    "  id: number;",
    "  user_id: number | null;",
    "  action: string;",
    "  ip_address: string | null;",
    "  details: string | null;",
    "  success: boolean;",
    "  created_at: string | null;",
    "}",
    "",
    "const ACTION_COLOR: Record<string, string> = {",
    "  LOGIN_SUCCESS: 'text-emerald-700 bg-emerald-50 border-emerald-200',",
    "  LOGIN_FAILED: 'text-red-700 bg-red-50 border-red-200',",
    "  LOGIN_BLOCKED: 'text-red-700 bg-red-50 border-red-200',",
    "  LOGOUT: 'text-gray-700 bg-gray-50 border-gray-200',",
    "  PASSWORD_CHANGED: 'text-blue-700 bg-blue-50 border-blue-200',",
    "  PASSWORD_FORCE_RESET: 'text-orange-700 bg-orange-50 border-orange-200',",
    "  USER_CREATED: 'text-purple-700 bg-purple-50 border-purple-200',",
    "  USER_ACTIVATED: 'text-emerald-700 bg-emerald-50 border-emerald-200',",
    "  USER_DEACTIVATED: 'text-red-700 bg-red-50 border-red-200',",
    "};",
    "",
    "function ActionBadge({ action }: { action: string }) {",
    "  const cls = ACTION_COLOR[action] ?? 'text-gray-700 bg-gray-50 border-gray-200';",
    "  return (",
    "    <span className={" + BT + "inline-flex items-center px-2 py-0.5 text-xs font-medium rounded-full border " + DOLLAR + "{cls}" + BT + "}>",
    "      {action.replace(/_/g, ' ')}",
    "    </span>",
    "  );",
    "}",
    "",
    "const COLUMNS: Column<AuditEntry>[] = [",
    "  {",
    "    key: 'action',",
    "    header: 'Action',",
    "    accessor: (r) => <ActionBadge action={r.action} />,",
    "    sortValue: (r) => r.action,",
    "  },",
    "  { key: 'user', header: 'User ID', accessor: (r) => r.user_id ?? '—', sortValue: (r) => r.user_id ?? 0 },",
    "  { key: 'ip', header: 'IP Address', accessor: (r) => r.ip_address ?? '—', sortValue: (r) => r.ip_address ?? '' },",
    "  {",
    "    key: 'status',",
    "    header: 'Result',",
    "    accessor: (r) => r.success",
    "      ? <span className=\"inline-flex items-center gap-1 text-xs text-emerald-700\"><CheckCircle2 size={12} />Success</span>",
    "      : <span className=\"inline-flex items-center gap-1 text-xs text-red-700\"><XCircle size={12} />Failed</span>,",
    "    sortValue: (r) => (r.success ? 1 : 0),",
    "  },",
    "  { key: 'details', header: 'Details', accessor: (r) => <span className=\"text-xs text-gray-600 truncate max-w-[220px] block\">{r.details ?? '—'}</span> },",
    "  {",
    "    key: 'time',",
    "    header: 'Time',",
    "    accessor: (r) => (",
    "      <span className=\"inline-flex items-center gap-1 text-xs text-gray-500\">",
    "        <Clock size={11} aria-hidden=\"true\" />",
    "        {r.created_at ? new Date(r.created_at).toLocaleString('en-IN') : '—'}",
    "      </span>",
    "    ),",
    "    sortValue: (r) => r.created_at ?? '',",
    "  },",
    "];",
    "",
    "export default function AuditLogs() {",
    "  const [data, setData] = useState<AuditEntry[]>([]);",
    "  const [loading, setLoading] = useState(true);",
    "  const [error, setError] = useState<string | null>(null);",
    "",
    "  const fetchLogs = () => {",
    "    setLoading(true);",
    "    setError(null);",
    "    getAuditLogs()",
    "      .then((res) => setData(res.data ?? []))",
    "      .catch((err: unknown) => setError((err as { message?: string })?.message ?? 'Failed to load audit logs.'))",
    "      .finally(() => setLoading(false));",
    "  };",
    "",
    "  useEffect(() => { fetchLogs(); }, []);",
    "",
    "  return (",
    "    <div className=\"flex flex-col gap-6\">",
    "      <PageHeader",
    "        title=\"Audit Logs\"",
    "        description=\"Security and activity audit trail — last 200 entries.\"",
    "        breadcrumbs={[{ label: 'Overview', to: '/dashboard' }, { label: 'Audit Logs' }]}",
    "        actions={",
    "          <button",
    "            onClick={fetchLogs}",
    "            disabled={loading}",
    "            className=\"inline-flex items-center gap-2 px-3 py-1.5 text-sm font-medium border border-gray-200 text-gray-700 bg-white rounded-md hover:bg-gray-50 transition-colors disabled:opacity-50\"",
    "          >",
    "            <RefreshCw size={14} className={loading ? 'animate-spin' : ''} aria-hidden=\"true\" />",
    "            Refresh",
    "          </button>",
    "        }",
    "      />",
    "",
    "      {!loading && data.length > 0 && (",
    "        <div className=\"flex gap-3 flex-wrap\">",
    "          {[",
    "            { label: 'Total', value: data.length, color: 'bg-gray-100 text-gray-700' },",
    "            { label: 'Successful', value: data.filter(d => d.success).length, color: 'bg-emerald-100 text-emerald-700' },",
    "            { label: 'Failed', value: data.filter(d => !d.success).length, color: 'bg-red-100 text-red-700' },",
    "            { label: 'Logins', value: data.filter(d => d.action.startsWith('LOGIN')).length, color: 'bg-blue-100 text-blue-700' },",
    "          ].map(s => (",
    "            <div key={s.label} className={" + BT + "px-3 py-1.5 rounded-full text-xs font-medium " + DOLLAR + "{s.color}" + BT + "}>",
    "              {s.label}: {s.value}",
    "            </div>",
    "          ))}",
    "        </div>",
    "      )}",
    "",
    "      {error ? (",
    "        <ErrorState message={error} onRetry={fetchLogs} />",
    "      ) : loading ? (",
    "        <TableSkeleton rows={10} cols={5} />",
    "      ) : !data.length ? (",
    "        <EmptyState",
    "          title=\"No audit entries\"",
    "          description=\"No security events have been recorded yet.\"",
    "          icon={<Shield size={40} strokeWidth={1.5} />}",
    "        />",
    "      ) : (",
    "        <DataTable",
    "          columns={COLUMNS}",
    "          data={data}",
    "          rowKey={(r) => String(r.id)}",
    "          searchable",
    "          searchPlaceholder=\"Search by action or IP...\"",
    "          searchFilter={(r, q) =>",
    "            r.action.toLowerCase().includes(q) ||",
    "            (r.ip_address ?? '').includes(q) ||",
    "            (r.details ?? '').toLowerCase().includes(q)",
    "          }",
    "          caption=\"Security audit log\"",
    "          defaultPageSize={50}",
    "        />",
    "      )}",
    "    </div>",
    "  );",
    "}",
    "",
]

(FE / "pages" / "AuditLogs.tsx").write_text(LF.join(lines), encoding='utf-8')
print("AuditLogs.tsx rewritten cleanly")